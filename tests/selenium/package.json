{
  "name": "techmart-selenium-tests",
  "version": "1.0.0",
  "description": "Selenium test suite for TechMart",

  "scripts": {
    "test": "mocha tests/**/*.spec.js --timeout 30000",
    "test:report": "mochawesome-report-generator reports/test-results.json"
  },
  
  "devDependencies": {
    "selenium-webdriver": "^4.18.1",
    "mocha": "^10.3.0",
    "chai": "^5.1.0",
    "mochawesome": "^7.1.3"
  }
}